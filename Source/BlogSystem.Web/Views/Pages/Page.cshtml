@model BlogSystem.Web.ViewModels.Pages.PageViewModel

@{
    ViewBag.Title = Model.Title;
}

@if (User.IsAdmin())
{
    <i class="fa fa-pencil"></i> <a href="/Administration/Pages/Edit/@Model.Id">Edit</a>
}

@Html.Raw(Model.Content)
